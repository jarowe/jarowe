/**
 * introMath — Stub placeholder
 * Full implementation exists on main worktree as unstaged changes.
 */
export function getIntroReveal() { return 1; }
export function getStaggeredReveal() { return 1; }
