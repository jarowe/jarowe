// HoverLabel — replaced with DOM-based tooltip (DomHoverLabel)
// 3D Text rendering crashed on degraded WebGL contexts (Context Lost at init).
// The DOM tooltip in ConstellationPage handles hover labels now.
export default function HoverLabel() {
  return null;
}
