---
phase: 09-visual-cohesion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/holidayCalendar.js
  - src/components/TodayRail.jsx
autonomous: true
requirements:
  - VISUAL-01

must_haves:
  truths:
    - "TodayRail Explore card shows the current holiday emoji and name"
    - "When today's holiday has a mapped nodeId, clicking Explore navigates to /constellation/{nodeId}"
    - "When today's holiday has no nodeId, the Explore link is hidden entirely -- no broken link or generic CTA"
    - "The existing TodayRail card layout (3-column grid) is preserved"
  artifacts:
    - path: "src/data/holidayCalendar.js"
      provides: "nodeId field on T3+ holiday entries"
      contains: "nodeId:"
    - path: "src/components/TodayRail.jsx"
      provides: "Conditional Explore link using holiday.nodeId"
      contains: "holiday.nodeId"
  key_links:
    - from: "src/components/TodayRail.jsx"
      to: "/constellation/:nodeId"
      via: "Link to={`/constellation/${holiday.nodeId}`}"
      pattern: "constellation.*nodeId"
    - from: "src/data/holidayCalendar.js"
      to: "public/data/constellation.graph.json"
      via: "nodeId values reference real constellation node IDs"
      pattern: "nodeId:\\s*['\"]ms-"
---

<objective>
Connect the TodayRail "Explore" card to the constellation by adding nodeId mappings to T3+ holidays and rendering a conditional deep-link in the Today State card.

Purpose: Visitors on celebration days (T3+) can jump directly from the homepage into the constellation focused on a thematically relevant node, creating a live connection between the daily layer and the 3D experience.

Output: Updated holidayCalendar.js with ~15-20 nodeId mappings on T3+ entries, and TodayRail.jsx with conditional "Explore in constellation" link.
</objective>

<execution_context>
@C:/Users/rowej/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rowej/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-visual-cohesion/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nodeId mappings to T3+ holidays in holidayCalendar.js</name>
  <files>src/data/holidayCalendar.js</files>
  <read_first>
    - src/data/holidayCalendar.js (current T3+ entries to modify)
    - public/data/constellation.graph.json (real node IDs to reference)
  </read_first>
  <action>
Add an optional `nodeId` field to T3+ holiday entries in holidayCalendar.js where a thematically relevant constellation node exists. Only map holidays where the connection is meaningful -- leave nodeId absent for holidays with no good match.

Use these exact mappings (verified against constellation.graph.json node IDs):

| Holiday Date | Holiday Name | nodeId | Reason |
|---|---|---|---|
| 01-01 | New Year's Day | ms-001 | Football injury redirects path -- new beginnings |
| 02-14 | Valentine's Day | ms-004 | Married Maria -- high school sweethearts |
| 03-03 | Jared's Birthday | cm-b-002 | Birthday moment -- Star Wars experience |
| 03-14 | Pi Day | ms-021 | Technology milestone -- CoD/Elgato collab |
| 04-12 | Cosmonautics Day | ms-020 | AI becomes primary creative lens -- tech frontier |
| 04-22 | Earth Day | ig-022 | Exploring ancient caves -- nature connection |
| 05-04 | Star Wars Day | cm-b-002 | Birthday Star Wars experience |
| 05-11 | Mother's Day | ig-039 | Happy Mother's Day tribute to Maria |
| 05-25 | Geek Pride Day | ms-010 | VBI -- democratizing broadcast for streamers |
| 06-15 | Father's Day | ms-006 | Jace born -- fatherhood begins |
| 06-21 | Summer Solstice | ig-015 | Off the coast of Syros -- summer travel |
| 07-04 | Independence Day | ms-014 | Starseed -- entrepreneurial independence |
| 07-08 | Video Game Day | cm-p-020 | Unity Game Design -- iOS |
| 07-20 | Moon Landing Day | ms-015 | Family moves to Greece -- giant leap |
| 08-16 | Rollercoaster Day | ig-019 | Boys dancing with the waves -- adventure |
| 09-08 | Star Trek Day | cm-p-009 | Interactive iOS App and DVD -- tech creativity |
| 09-13 | Programmers Day | cm-p-007 | Code Based Animation -- SVG |
| 10-31 | Halloween | fb-1196 | Boys in costume -- fatherhood moment |
| 11-27 | Thanksgiving | ms-016 | Boundless Life community -- found a global family |
| 12-25 | Christmas Day | ms-004 | Marriage milestone -- family |

Add the `nodeId` field inline after the existing fields on each entry. For example, the Jan 1 entry becomes:
```
'01-01': { tier: 3, name: "New Year's Day", category: 'humor', emoji: '...', ..., nodeId: 'ms-001' },
```

Do NOT modify entries that are not in the mapping table above. Holidays like Pizza Day, Pokemon Day, April Fools, Alien Day, etc. get no nodeId -- they have no meaningful constellation connection.
  </action>
  <verify>
    <automated>node -e "const c=require('./src/data/holidayCalendar.js');const h=c.HOLIDAY_CALENDAR;const mapped=Object.entries(h).filter(([k,v])=>v.nodeId);console.log('Mapped:',mapped.length);if(mapped.length<15)throw new Error('Expected >=15 mapped holidays');mapped.forEach(([k,v])=>{if(!v.nodeId.match(/^(ms|cm|ig|fb)-/))throw new Error('Invalid nodeId: '+v.nodeId)});console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - holidayCalendar.js contains `nodeId: 'ms-001'` on the '01-01' entry
    - holidayCalendar.js contains `nodeId: 'ms-004'` on the '02-14' entry
    - holidayCalendar.js contains `nodeId: 'ms-006'` on the '06-15' entry
    - At least 15 T3+ entries have a nodeId field
    - No T1 or T2 entries have a nodeId field
    - All nodeId values match IDs that exist in constellation.graph.json
  </acceptance_criteria>
  <done>T3+ holidays have thematically mapped nodeId fields pointing to real constellation node IDs; unmapped holidays have no nodeId field</done>
</task>

<task type="auto">
  <name>Task 2: Add conditional Explore link to TodayRail Today State card</name>
  <files>src/components/TodayRail.jsx</files>
  <read_first>
    - src/components/TodayRail.jsx (current component to modify)
    - src/components/TodayRail.css (existing styles for consistency)
    - src/data/holidayCalendar.js (after Task 1 -- nodeId field available on holiday object)
  </read_first>
  <action>
Modify the Today State card (Card 1) in TodayRail.jsx to show a constellation deep-link when the current holiday has a nodeId.

1. **Replace the static "Explore today" link** with a conditional block:
   - When `holiday && holiday.nodeId`: show a `<Link to={'/constellation/' + holiday.nodeId}>` with text "Explore in constellation" and the ArrowRight icon
   - When `holiday && !holiday.nodeId`: show the holiday info (emoji + name) but NO explore link -- just the holiday display without any CTA below it
   - When no holiday: keep existing behavior (the generic "Explore today" link to /constellation is fine as a fallback)

2. **Remove the FEATURED_NODES array and featuredNode logic** entirely. The `todayData.featuredNode` paragraph (`<p className="today-card__featured">`) was a placeholder. Replace it:
   - When `holiday && holiday.nodeId`: do not show the old featuredNode text -- the explore CTA is sufficient
   - When `holiday && !holiday.nodeId`: optionally show the holiday greeting as the descriptive text instead (`holiday.greeting`)
   - When no holiday: show a generic fallback like "Explore the constellation"

3. **Keep all other card behavior unchanged**: date display, holiday emoji/name, Framer Motion animation, CSS classes.

The resulting Card 1 JSX should look approximately like:
```jsx
{/* Card 1: Today State */}
<motion.div className="today-card today-card--state" ...>
  <div className="today-card__date">
    <Calendar size={14} className="today-card__date-icon" />
    <span>{todayData.dateStr}</span>
  </div>
  {holiday && holiday.name && (
    <div className="today-card__holiday">
      <span className="today-card__emoji">{holiday.emoji}</span>
      <span>{holiday.name}</span>
    </div>
  )}
  {holiday && holiday.nodeId ? (
    <Link to={`/constellation/${holiday.nodeId}`} className="today-card__cta">
      Explore in constellation <ArrowRight size={14} />
    </Link>
  ) : holiday && holiday.greeting ? (
    <p className="today-card__featured">{holiday.greeting}</p>
  ) : (
    <Link to="/constellation" className="today-card__cta">
      Explore today <ArrowRight size={14} />
    </Link>
  )}
</motion.div>
```

4. **Clean up unused imports**: Remove the `FEATURED_NODES` const. The `dailyPick` import may still be needed for other cards (check before removing). Remove `featuredNode` from `todayData` useMemo.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/components/TodayRail.jsx','utf8');if(s.includes('FEATURED_NODES'))throw new Error('FEATURED_NODES not removed');if(!s.includes('holiday.nodeId'))throw new Error('Missing holiday.nodeId check');if(!s.includes('/constellation/'))throw new Error('Missing constellation deep-link');if(!s.includes('Explore in constellation'))throw new Error('Missing Explore in constellation text');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - TodayRail.jsx contains `holiday.nodeId` conditional check
    - TodayRail.jsx contains string "Explore in constellation"
    - TodayRail.jsx contains template literal `/constellation/${holiday.nodeId}`
    - TodayRail.jsx does NOT contain `FEATURED_NODES` array
    - TodayRail.jsx does NOT contain `todayData.featuredNode`
    - When holiday has no nodeId, no "Explore in constellation" link renders (greeting or generic fallback instead)
    - Existing Glint Journal card (Card 2) and Creative Prompt card (Card 3) are unchanged
  </acceptance_criteria>
  <done>Today State card conditionally shows "Explore in constellation" deep-link for holidays with nodeId; holidays without nodeId show greeting text; no-holiday state falls back to generic explore link</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. On a T3+ holiday with nodeId (e.g., change system date or test with '01-01'): TodayRail shows "Explore in constellation" link pointing to `/constellation/ms-001`
3. On a T3+ holiday without nodeId (e.g., '02-09' Pizza Day): TodayRail shows holiday emoji + name + greeting text, NO explore link
4. On a T1 holiday: TodayRail shows holiday info + generic "Explore today" link to /constellation
5. All three TodayRail cards render correctly (Today State, Glint Journal, Creative Prompt)
</verification>

<success_criteria>
- At least 15 T3+ holidays in holidayCalendar.js have nodeId fields pointing to real constellation node IDs
- TodayRail conditionally renders "Explore in constellation" deep-link only for holidays with nodeId
- Unmapped holidays degrade gracefully (show greeting or generic link, never broken/empty)
- No build errors, no runtime console errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-visual-cohesion/09-01-SUMMARY.md`
</output>
