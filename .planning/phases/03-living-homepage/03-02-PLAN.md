---
phase: 03-living-homepage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Starseed.jsx
  - src/pages/Starseed.css
  - src/App.jsx
  - src/components/Navbar.jsx
  - src/components/Navbar.css
autonomous: true
requirements: [STAR-01, STAR-03, STAR-04]

must_haves:
  truths:
    - "Navigating to /starseed shows a branded shell page with Starseed identity (distinct fonts, colors, and chrome from jarowe.com)"
    - "A clear back/escape navigation returns the visitor from /starseed to the main jarowe.com homepage"
    - "The Starseed route uses the campaign-shell pattern with its own nav, palette, and visual identity"
    - "Navigating to /starseed does not load heavy components from other routes"
  artifacts:
    - path: "src/pages/Starseed.jsx"
      provides: "Branded Starseed hub shell with campaign-shell pattern"
      min_lines: 80
    - path: "src/pages/Starseed.css"
      provides: "Starseed-specific brand styling (fonts, colors, layout)"
      min_lines: 60
    - path: "src/App.jsx"
      provides: "Route registration for /starseed"
      contains: "path=\"/starseed\""
  key_links:
    - from: "src/App.jsx"
      to: "src/pages/Starseed.jsx"
      via: "React Router Route element"
      pattern: "<Route.*path=./starseed"
    - from: "src/components/Navbar.jsx"
      to: "/starseed"
      via: "Navigation link"
      pattern: "starseed"
    - from: "src/pages/Starseed.jsx"
      to: "/"
      via: "Escape hatch Link component"
      pattern: "to=./."
---

<objective>
Create the `/starseed` branded hub route as a standalone shell with its own visual identity distinct from the main jarowe.com aesthetic.

Purpose: Starseed is the creation-facing satellite of jarowe.com. It needs its own branded surface that feels premium but clearly distinct from the main site -- like a music release's campaign shell pattern (proven from the BITB takeover system). This plan establishes the route, shell chrome, visual identity, and escape-hatch navigation. Project cards and content come in Phase 5.

Output: A `/starseed` route with Starseed branding (fonts, colors, chrome), a hero section establishing the brand, placeholder project slots, and seamless return navigation to jarowe.com.
</objective>

<execution_context>
@C:/Users/rowej/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rowej/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-living-homepage/UI-SPEC.md
@.planning/future/MASTER_PLAN.md
@.planning/future/IDENTITY.md
@.planning/future/ORBIT_MODEL.md
@src/App.jsx
@src/components/Navbar.jsx
@src/pages/StarseedProject.jsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/App.jsx (route registration pattern):
```javascript
// Normal site routes are registered inside <Routes> in AppContent()
// Lazy-loaded pages use lazyRetry() wrapper
// Route pattern: <Route path="/path" element={<Component />} />

// Campaign shell pattern (proven):
const ReleaseShell = lazyRetry(() => import('./pages/release/ReleaseShell'));
// ReleaseShell wraps campaign pages with own nav, palette, chrome rules
```

From src/components/Navbar.jsx (navigation pattern):
```javascript
// Navbar renders <Link> components for site navigation
// Links use react-router-dom <Link to="/path">
// Navbar is hidden on release routes via chrome.hideNavbar
```

From src/content/takeovers/registry.js (campaign-shell pattern reference):
```javascript
// Each campaign has: id, pages (landing/artist/epk), loadConfig
// Config includes chrome rules: hideNavbar, hideGlobalPlayer, disableHolidayBodyFx
// This pattern is what STAR-01 means by "campaign-shell pattern"
```

From .planning/future/IDENTITY.md (Starseed brand positioning):
- "jarowe.com inspires. Starseed helps you begin."
- Starseed = creation-facing satellite with own identity
- Should feel premium but clearly distinct from jarowe.com
- Use catalytic language: "Start in Starseed", "Sketch the idea", "Open a canvas"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Starseed branded hub page with campaign-shell pattern</name>
  <files>src/pages/Starseed.jsx, src/pages/Starseed.css</files>
  <read_first>
    - src/pages/StarseedProject.jsx (existing Starseed project page -- will be replaced/superseded)
    - src/pages/Workshop.jsx (existing Workshop page that Starseed replaces on the homepage cell)
    - src/pages/Workshop.css (existing Workshop styling for reference)
    - src/pages/release/ReleaseShell.jsx (campaign-shell pattern reference -- this is the proven pattern for pages with their own chrome)
    - src/components/Navbar.jsx (understand current nav structure)
    - src/components/Navbar.css (understand nav styling)
    - .planning/phases/03-living-homepage/UI-SPEC.md (section 11 -- Starseed CTA contract)
    - .planning/future/IDENTITY.md (brand positioning)
    - .planning/future/ORBIT_MODEL.md (Orbit 3 -- Creation Layer)
  </read_first>
  <action>
**1. Create `src/pages/Starseed.jsx`:**

Build a branded hub page with its own visual identity. The page has its own internal navigation chrome (not the main Navbar) and uses the campaign-shell pattern where the parent route manages its own look and feel.

Structure:
```jsx
import { motion } from 'framer-motion';
import { ArrowLeft, Sparkles, PenTool, Palette, Lightbulb, ExternalLink } from 'lucide-react';
import { Link, useNavigate } from 'react-router-dom';
import './Starseed.css';

export default function Starseed() {
  const navigate = useNavigate();

  return (
    <div className="starseed-shell" data-brand="starseed">
      {/* Starseed internal nav */}
      <nav className="starseed-nav">
        <Link to="/" className="starseed-escape">
          <ArrowLeft size={16} />
          <span>Back to jarowe.com</span>
        </Link>
        <div className="starseed-brand">
          <Sparkles size={18} className="starseed-logo-icon" />
          <span className="starseed-wordmark">Starseed</span>
        </div>
      </nav>

      {/* Hero section */}
      <motion.section className="starseed-hero"
        initial={{ opacity: 0, y: 20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.6 }}
      >
        <h1 className="starseed-title">
          Where ideas<br />become real.
        </h1>
        <p className="starseed-subtitle">
          Creative studio. Tools. Experiments. A launchpad for what's next.
        </p>
      </motion.section>

      {/* Project grid -- placeholder slots for Phase 5 project cards */}
      <section className="starseed-projects">
        <h2 className="starseed-section-heading">Active Projects</h2>
        <div className="starseed-project-grid">
          {/* BEAMY */}
          <motion.div className="starseed-card"
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.15 }}
            onClick={() => navigate('/projects/beamy')}
          >
            <div className="starseed-card__icon"><PenTool size={28} /></div>
            <h3>BEAMY</h3>
            <p>Simply powerful animation engine. C++ core, React frontend.</p>
            <div className="starseed-card__tags">
              <span className="starseed-tag">Animation</span>
              <span className="starseed-tag">Desktop</span>
            </div>
          </motion.div>

          {/* AMINA */}
          <motion.div className="starseed-card"
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.25 }}
          >
            <div className="starseed-card__icon"><Lightbulb size={28} /></div>
            <h3>AMINA</h3>
            <p>AI-native creative assistant. Idea capture meets intelligent expansion.</p>
            <div className="starseed-card__tags">
              <span className="starseed-tag">AI</span>
              <span className="starseed-tag">Creative</span>
            </div>
            <span className="starseed-card__status">Coming soon</span>
          </motion.div>

          {/* Starseed Labs */}
          <motion.div className="starseed-card"
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.35 }}
          >
            <div className="starseed-card__icon"><Palette size={28} /></div>
            <h3>Starseed Labs</h3>
            <p>Scratchpad. Canvas. Brainstorm. Creation tools for the curious.</p>
            <div className="starseed-card__tags">
              <span className="starseed-tag">Tools</span>
              <span className="starseed-tag">Creative</span>
            </div>
            <span className="starseed-card__status">Coming soon</span>
          </motion.div>
        </div>
      </section>

      {/* Footer CTA */}
      <section className="starseed-footer-cta">
        <p>Starseed is a satellite of <Link to="/" className="starseed-link">jarowe.com</Link> -- a living creative world.</p>
      </section>
    </div>
  );
}
```

**2. Create `src/pages/Starseed.css`:**

Brand identity that is visually distinct from jarowe.com:

- **Background**: Slightly warmer/lighter dark than jarowe.com's pure `#050505`. Use `#080810` with a subtle warm gradient.
- **Accent color**: Starseed gold `#dbb978` (warm gold) instead of jarowe.com purple `#7c3aed`.
- **Secondary accent**: Starseed copper `#c7956d` instead of electric blue.
- **Font**: Keep `var(--font-display)` (Outfit) for headings but use `letter-spacing: 0.04em` and `font-weight: 700` (slightly different character than jarowe.com's 800).
- **Glass panels**: Warmer tint (gold-tinted glass instead of purple-tinted).
- **Border**: `rgba(219, 185, 120, 0.12)` instead of white.

Key CSS rules:

```css
.starseed-shell {
  min-height: 100vh;
  background: #080810;
  color: #f0f0f0;
  padding-bottom: 4rem;
}

/* Override jarowe.com ambient glows when inside starseed */
.starseed-shell::before {
  content: "";
  position: fixed;
  inset: 0;
  background: radial-gradient(ellipse at 30% 20%, rgba(219,185,120,0.06) 0%, transparent 50%),
              radial-gradient(ellipse at 70% 80%, rgba(199,149,109,0.04) 0%, transparent 50%);
  z-index: 0;
  pointer-events: none;
}

.starseed-nav {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1rem 2rem;
  position: sticky;
  top: 0;
  z-index: 100;
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  background: rgba(8,8,16,0.8);
  border-bottom: 1px solid rgba(219,185,120,0.08);
}

.starseed-escape {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 0.35rem 0.85rem;
  background: rgba(255,255,255,0.04);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 100px;
  font-size: 0.75rem;
  color: #888;
  text-decoration: none;
  transition: all 0.2s ease;
}

.starseed-escape:hover {
  background: rgba(219,185,120,0.1);
  border-color: rgba(219,185,120,0.25);
  color: #dbb978;
}

.starseed-brand {
  display: flex;
  align-items: center;
  gap: 8px;
}

.starseed-logo-icon {
  color: #dbb978;
}

.starseed-wordmark {
  font-family: var(--font-display);
  font-weight: 700;
  font-size: 1.1rem;
  letter-spacing: 0.04em;
  color: #dbb978;
}

.starseed-hero {
  max-width: 800px;
  margin: 4rem auto 3rem;
  padding: 0 2rem;
  text-align: center;
  position: relative;
  z-index: 1;
}

.starseed-title {
  font-family: var(--font-display);
  font-weight: 700;
  font-size: clamp(2.5rem, 5vw, 4rem);
  letter-spacing: 0.02em;
  line-height: 1.1;
  background: linear-gradient(135deg, #dbb978 0%, #f0d9a8 40%, #c7956d 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 1rem;
}

.starseed-subtitle {
  font-size: 1.15rem;
  color: #9999a1;
  line-height: 1.6;
  max-width: 500px;
  margin: 0 auto;
}

.starseed-projects {
  max-width: 1000px;
  margin: 0 auto;
  padding: 0 2rem;
  position: relative;
  z-index: 1;
}

.starseed-section-heading {
  font-family: var(--font-display);
  font-weight: 700;
  font-size: 1.2rem;
  color: #dbb978;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  margin-bottom: 1.5rem;
}

.starseed-project-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 1.5rem;
}

.starseed-card {
  background: rgba(219,185,120,0.04);
  border: 1px solid rgba(219,185,120,0.1);
  border-radius: 20px;
  padding: 2rem;
  cursor: pointer;
  transition: all 0.3s ease;
  position: relative;
}

.starseed-card:hover {
  border-color: rgba(219,185,120,0.25);
  transform: translateY(-2px);
  box-shadow: 0 8px 30px rgba(219,185,120,0.08);
}

.starseed-card__icon {
  color: #dbb978;
  margin-bottom: 1rem;
}

.starseed-card h3 {
  font-family: var(--font-display);
  font-weight: 700;
  font-size: 1.3rem;
  margin-bottom: 0.5rem;
}

.starseed-card p {
  color: #9999a1;
  font-size: 0.9rem;
  line-height: 1.5;
  margin-bottom: 1rem;
}

.starseed-card__tags {
  display: flex;
  gap: 0.5rem;
  flex-wrap: wrap;
}

.starseed-tag {
  padding: 0.2rem 0.6rem;
  background: rgba(219,185,120,0.08);
  border: 1px solid rgba(219,185,120,0.12);
  border-radius: 100px;
  font-size: 0.7rem;
  color: #dbb978;
  letter-spacing: 0.03em;
}

.starseed-card__status {
  position: absolute;
  top: 1rem;
  right: 1rem;
  font-size: 0.65rem;
  color: #666;
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

.starseed-footer-cta {
  text-align: center;
  margin-top: 4rem;
  padding: 2rem;
  color: #666;
  font-size: 0.85rem;
  position: relative;
  z-index: 1;
}

.starseed-link {
  color: #a78bfa;
  text-decoration: underline;
  text-underline-offset: 2px;
}

.starseed-link:hover {
  color: #c4b5fd;
}

/* Responsive */
@media (max-width: 768px) {
  .starseed-hero {
    margin-top: 2rem;
  }
  .starseed-nav {
    padding: 0.75rem 1rem;
  }
  .starseed-project-grid {
    grid-template-columns: 1fr;
  }
}
```
  </action>
  <verify>
    <automated>cd C:/dev/jarowe && node -e "
      // Check file exists and exports default component
      import('./src/pages/Starseed.jsx').then(m => {
        console.assert(typeof m.default === 'function', 'Starseed should export default function');
        console.log('Starseed.jsx exports verified');
      }).catch(e => console.log('ESM check skipped:', e.message));
    " 2>&1; test -f C:/dev/jarowe/src/pages/Starseed.css && echo "Starseed.css exists" || echo "MISSING: Starseed.css"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/Starseed.jsx exists and exports a default function component
    - src/pages/Starseed.jsx contains `className="starseed-shell"` with `data-brand="starseed"`
    - src/pages/Starseed.jsx contains a Link component with `to="/"` for escape-hatch navigation
    - src/pages/Starseed.jsx contains `className="starseed-nav"` with sticky positioning
    - src/pages/Starseed.jsx contains `className="starseed-wordmark"` with text "Starseed"
    - src/pages/Starseed.jsx contains at least 2 project card placeholders (BEAMY and Starseed Labs)
    - src/pages/Starseed.css exists and is at least 60 lines
    - src/pages/Starseed.css contains `.starseed-title` with `background: linear-gradient` using gold tones (#dbb978)
    - src/pages/Starseed.css contains `.starseed-escape` styled as a pill (border-radius: 100px)
    - src/pages/Starseed.css contains `@media (max-width: 768px)` responsive breakpoint
    - Starseed accent color is gold (#dbb978), NOT purple (#7c3aed) -- distinct from jarowe.com
  </acceptance_criteria>
  <done>Starseed branded hub page exists with its own visual identity (gold accent, warm dark background, distinct chrome), project card placeholders, and escape-hatch back to jarowe.com.</done>
</task>

<task type="auto">
  <name>Task 2: Register Starseed route in App.jsx and add Starseed navigation link</name>
  <files>src/App.jsx, src/components/Navbar.jsx, src/components/Navbar.css</files>
  <read_first>
    - src/App.jsx (full file -- route registration pattern, lazy loading pattern, chrome rules)
    - src/components/Navbar.jsx (full file -- current navigation links, structure)
    - src/components/Navbar.css (full file -- current nav styling)
    - src/pages/Starseed.jsx (just created in Task 1 -- verify import path)
  </read_first>
  <action>
**1. Register `/starseed` route in `src/App.jsx`:**

Add lazy import at the top of App.jsx (near other lazy imports around line 25-34):
```javascript
const Starseed = lazyRetry(() => import('./pages/Starseed'));
```

Add route inside the `<Routes>` block, in the "Normal site routes" section (after the workshop route around line 237):
```jsx
<Route path="/starseed" element={
  <Suspense fallback={<LazyFallback label="Loading Starseed..." />}>
    <Starseed />
  </Suspense>
} />
```

The Starseed page has its own internal nav (starseed-nav), so the main site Navbar should still render. However, the Starseed shell manages its own chrome internally. The main Navbar showing is fine -- it provides site-wide context and the Starseed page scrolls below it.

**Actually, the cleaner approach**: Since the Starseed shell has its own nav with escape hatch, hide the main Navbar when on `/starseed` routes. In the `AppContent` function, detect Starseed routes:

```javascript
const isStarseedRoute = location.pathname.startsWith('/starseed');
```

Then modify the Navbar rendering:
```jsx
{!chrome.hideNavbar && !isStarseedRoute && <Navbar />}
```

This gives Starseed full page control like the release shell pattern.

**2. Add Starseed link to `src/components/Navbar.jsx`:**

Find the navigation links section. Add a "Starseed" link that navigates to `/starseed`. Place it after the existing main links but before any utility icons. Use the Sparkles icon from lucide-react.

Add import at top:
```javascript
import { Sparkles } from 'lucide-react'; // add to existing lucide imports
```

Add link in the nav links area:
```jsx
<Link to="/starseed" className="nav-link starseed-nav-link" onClick={playClickSound} onMouseEnter={playHoverSound}>
  <Sparkles size={14} />
  <span>Starseed</span>
</Link>
```

**3. Style the Starseed nav link in `src/components/Navbar.css`:**

Add a subtle gold accent to differentiate the Starseed link from other nav links:

```css
.starseed-nav-link {
  color: #dbb978;
  opacity: 0.7;
  transition: opacity 0.2s ease, color 0.2s ease;
}

.starseed-nav-link:hover {
  opacity: 1;
  color: #f0d9a8;
}
```

**4. Update the Workshop cell in Home.jsx to point to Starseed:**

In `src/pages/Home.jsx`, find the Workshop cell (around line 6715-6723):
```jsx
{/* WORKSHOP CELL */}
<div className="bento-cell cell-project clickable" onClick={() => navigate('/workshop')}>
```

Change:
- `onClick` from `/workshop` to `/starseed`
- Title from "The Workshop" to "Starseed"
- Subtitle from "SD Patcher, BEAMY, & Experiments." to "Where ideas become real."
- Badge from "Tools & Builds" to "Create"

The cell should become:
```jsx
{/* STARSEED CELL */}
<div className="bento-cell cell-project clickable" onClick={() => navigate('/starseed')}>
  <div className="project-image" style={{ backgroundImage: `url(${BASE}images/tools-builds-bg.png)`, filter: 'brightness(0.6) saturate(0.8) sepia(0.15)' }}></div>
  <div className="featured-badge" style={{ background: 'linear-gradient(135deg, rgba(219,185,120,0.15), rgba(199,149,109,0.1))', color: '#dbb978', borderColor: 'rgba(219,185,120,0.2)' }}>Create</div>
  <div className="bento-content" style={{ zIndex: 1 }}>
    <h3 className="project-title" style={{ fontSize: '1.8rem', marginBottom: '0.2rem' }}>Starseed</h3>
    <p style={{ color: '#eee', fontSize: '0.95rem' }}>Where ideas become real.</p>
  </div>
</div>
```

Keep the existing `cell-project` CSS class and background image (tools-builds-bg.png) for now -- the sepia filter gives it a warmer Starseed tone. Phase 5 can provide a proper Starseed hero image.
  </action>
  <verify>
    <automated>cd C:/dev/jarowe && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/App.jsx contains `const Starseed = lazyRetry(() => import('./pages/Starseed'))`
    - src/App.jsx contains `<Route path="/starseed"` with Suspense wrapper
    - src/App.jsx contains `isStarseedRoute` detection and Navbar is hidden on /starseed
    - src/components/Navbar.jsx contains a Link with `to="/starseed"`
    - src/components/Navbar.jsx imports Sparkles from lucide-react (either in existing import or new)
    - src/components/Navbar.css contains `.starseed-nav-link` with `color: #dbb978`
    - src/pages/Home.jsx Workshop cell now has onClick navigating to '/starseed' instead of '/workshop'
    - src/pages/Home.jsx Workshop cell title says "Starseed" not "The Workshop"
    - Build completes without errors (vite build exits 0)
  </acceptance_criteria>
  <done>The /starseed route is registered and lazy-loaded, the main Navbar hides on Starseed routes (campaign-shell pattern), a Starseed nav link exists in the Navbar, and the homepage Workshop cell now points to /starseed with Starseed branding.</done>
</task>

</tasks>

<verification>
1. `npx vite build` completes without errors
2. Navigating to `/starseed` in the dev server shows the Starseed branded page
3. The main site Navbar is hidden on `/starseed` -- Starseed has its own nav
4. Clicking "Back to jarowe.com" on the Starseed page returns to the homepage
5. The homepage Workshop cell now says "Starseed" and navigates to `/starseed`
6. The Starseed page uses gold accent (#dbb978), not purple (#7c3aed)
7. The Navbar has a "Starseed" link with a sparkles icon
</verification>

<success_criteria>
- /starseed route renders a branded Starseed shell with gold accent colors, warm dark background, and its own navigation chrome
- Escape hatch (Back to jarowe.com) link returns to homepage via React Router Link
- Main Navbar hidden on /starseed (campaign-shell pattern)
- Homepage Workshop cell repurposed to Starseed with correct navigation
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-living-homepage/03-02-SUMMARY.md`
</output>
