---
phase: 05-starseed-hub-labs
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/pages/labs/LabsHub.jsx
  - src/pages/labs/LabsHub.css
  - src/App.jsx
  - src/components/TodayRail.jsx
autonomous: false
requirements: [LABS-06]

must_haves:
  truths:
    - "Visitor navigating to /starseed/labs sees a hub page with 3 glass cards: Scratchpad, Canvas, and Brainstorm"
    - "Scratchpad and Canvas cards link to their respective routes and are clickable"
    - "Brainstorm card shows a 'Coming soon' badge and is not clickable"
    - "Homepage 'Start in Starseed' CTA links to /starseed/labs/scratchpad with the creative prompt as a query param"
  artifacts:
    - path: "src/pages/labs/LabsHub.jsx"
      provides: "Labs hub page with 3 glass cards under Starseed shell"
      contains: "starseed-shell"
    - path: "src/pages/labs/LabsHub.css"
      provides: "Glass card grid styling for Labs hub"
      contains: "labs-hub"
    - path: "src/App.jsx"
      provides: "Lazy-loaded /starseed/labs route"
      contains: "starseed/labs"
    - path: "src/components/TodayRail.jsx"
      provides: "Updated CTA linking to scratchpad with prompt param"
      contains: "starseed/labs/scratchpad"
  key_links:
    - from: "src/pages/labs/LabsHub.jsx"
      to: "/starseed/labs/scratchpad"
      via: "Link component"
      pattern: "to=.*starseed/labs/scratchpad"
    - from: "src/pages/labs/LabsHub.jsx"
      to: "/starseed/labs/canvas"
      via: "Link component"
      pattern: "to=.*starseed/labs/canvas"
    - from: "src/components/TodayRail.jsx"
      to: "/starseed/labs/scratchpad"
      via: "Link with prompt query param"
      pattern: "starseed/labs/scratchpad.*prompt.*encodeURIComponent"
---

<objective>
Create the Labs hub page with entry point cards and update the TodayRail CTA to link directly to the scratchpad with the creative prompt pre-loaded.

Purpose: Give visitors a clear entry point to all Labs tools and connect the daily creative prompt flow to the scratchpad.
Output: Labs hub page with 3 cards, LabsHub route in App.jsx, updated TodayRail CTA.
</objective>

<execution_context>
@C:/Users/rowej/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rowej/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-starseed-hub-labs/05-RESEARCH.md
@.planning/phases/05-starseed-hub-labs/05-02-SUMMARY.md

<interfaces>
<!-- From Plan 02: routes that must exist before this plan runs -->

From src/App.jsx (after Plan 02):
```jsx
// These routes already exist from Plan 02:
<Route path="/starseed/labs/scratchpad" element={<Suspense ...><Scratchpad /></Suspense>} />
<Route path="/starseed/labs/canvas" element={<Suspense ...><LabsCanvas /></Suspense>} />

// This plan adds:
<Route path="/starseed/labs" element={<Suspense ...><LabsHub /></Suspense>} />
```

From src/components/TodayRail.jsx:
```jsx
// Current CTA (line 133):
<Link to="/starseed" className="today-card__cta today-card__cta--starseed">
  Start in Starseed <ArrowRight size={14} />
</Link>

// todayData.prompt.text contains the prompt string to encode
```

From src/pages/Starseed.css (nav chrome styles imported by Labs pages):
```css
/* These classes are available for Labs pages that import Starseed.css:
   .starseed-shell, .starseed-nav, .starseed-escape, .starseed-brand,
   .starseed-logo-icon, .starseed-wordmark */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LabsHub page with 3 glass cards, wire route in App.jsx, and update TodayRail CTA</name>
  <files>src/pages/labs/LabsHub.jsx, src/pages/labs/LabsHub.css, src/App.jsx, src/components/TodayRail.jsx</files>

  <read_first>
    - src/App.jsx (current state after Plan 02 -- need to add LabsHub lazy import + route)
    - src/components/TodayRail.jsx (find the "Start in Starseed" CTA Link to update)
    - src/pages/Starseed.jsx (shell nav pattern to replicate)
    - src/pages/Starseed.css (imported for nav chrome consistency)
  </read_first>

  <action>
**1. Create `src/pages/labs/LabsHub.jsx`:**

Hub page with 3 glass cards under the Starseed shell:

```jsx
import { motion } from 'framer-motion';
import { ArrowLeft, Sparkles, PenTool, Palette, Lightbulb, ArrowRight } from 'lucide-react';
import { Link } from 'react-router-dom';
import '../Starseed.css';
import './LabsHub.css';

const LABS_TOOLS = [
  {
    id: 'scratchpad',
    name: 'Scratchpad',
    description: 'Markdown editor. Jot ideas, draft plans, capture thoughts with rich formatting.',
    icon: PenTool,
    url: '/starseed/labs/scratchpad',
    status: 'active',
  },
  {
    id: 'canvas',
    name: 'Canvas',
    description: 'Infinite drawing surface. Sketch, diagram, map out ideas with freeform tools.',
    icon: Palette,
    url: '/starseed/labs/canvas',
    status: 'active',
  },
  {
    id: 'brainstorm',
    name: 'Brainstorm',
    description: 'AI-powered ideation. Collaborate with Glint to develop project briefs and creative direction.',
    icon: Lightbulb,
    url: null,
    status: 'coming-soon',
  },
];

export default function LabsHub() {
  return (
    <div className="starseed-shell" data-brand="starseed">
      <nav className="starseed-nav">
        <Link to="/starseed" className="starseed-escape">
          <ArrowLeft size={16} />
          <span>Back to Starseed</span>
        </Link>
        <div className="starseed-brand">
          <Sparkles size={18} className="starseed-logo-icon" />
          <span className="starseed-wordmark">Labs</span>
        </div>
      </nav>

      <motion.section className="labs-hub-hero"
        initial={{ opacity: 0, y: 20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.6 }}
      >
        <h1 className="labs-hub-title">Creation Tools</h1>
        <p className="labs-hub-subtitle">
          Write, sketch, and brainstorm. Your workspace inside the Starseed universe.
        </p>
      </motion.section>

      <section className="labs-hub-grid-section">
        <div className="labs-hub-grid">
          {LABS_TOOLS.map((tool, index) => {
            const Icon = tool.icon;
            const isActive = tool.status === 'active' && tool.url;

            const cardContent = (
              <motion.div
                className={`labs-hub-card ${!isActive ? 'labs-hub-card--disabled' : ''}`}
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ delay: 0.15 * (index + 1) }}
                key={tool.id}
              >
                <div className="labs-hub-card__icon">
                  <Icon size={32} />
                </div>
                <h3 className="labs-hub-card__name">{tool.name}</h3>
                <p className="labs-hub-card__desc">{tool.description}</p>
                {tool.status === 'coming-soon' && (
                  <span className="labs-hub-card__status">Coming soon</span>
                )}
                {isActive && (
                  <div className="labs-hub-card__action">
                    Open <ArrowRight size={14} />
                  </div>
                )}
              </motion.div>
            );

            return isActive ? (
              <Link to={tool.url} key={tool.id} className="labs-hub-card-link">
                {cardContent}
              </Link>
            ) : (
              <div key={tool.id}>{cardContent}</div>
            );
          })}
        </div>
      </section>
    </div>
  );
}
```

**2. Create `src/pages/labs/LabsHub.css`:**

```css
.labs-hub-hero {
  max-width: 700px;
  margin: 4rem auto 3rem;
  padding: 0 2rem;
  text-align: center;
  position: relative;
  z-index: 1;
}

.labs-hub-title {
  font-family: var(--font-display);
  font-weight: 700;
  font-size: clamp(2rem, 4vw, 3rem);
  background: linear-gradient(135deg, #dbb978 0%, #f0d9a8 40%, #c7956d 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 0.75rem;
}

.labs-hub-subtitle {
  font-size: 1.05rem;
  color: #9999a1;
  line-height: 1.6;
}

.labs-hub-grid-section {
  max-width: 1000px;
  margin: 0 auto;
  padding: 0 2rem;
  position: relative;
  z-index: 1;
}

.labs-hub-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 1.5rem;
}

.labs-hub-card-link {
  text-decoration: none;
  color: inherit;
}

.labs-hub-card {
  background: rgba(219,185,120,0.04);
  border: 1px solid rgba(219,185,120,0.1);
  border-radius: 20px;
  padding: 2.5rem 2rem;
  transition: all 0.3s ease;
  position: relative;
  text-align: center;
}

.labs-hub-card-link:hover .labs-hub-card {
  border-color: rgba(219,185,120,0.25);
  transform: translateY(-2px);
  box-shadow: 0 8px 30px rgba(219,185,120,0.08);
}

.labs-hub-card--disabled {
  opacity: 0.5;
  cursor: default;
}

.labs-hub-card__icon {
  color: #dbb978;
  margin-bottom: 1.25rem;
}

.labs-hub-card__name {
  font-family: var(--font-display);
  font-weight: 700;
  font-size: 1.4rem;
  margin-bottom: 0.6rem;
}

.labs-hub-card__desc {
  color: #9999a1;
  font-size: 0.9rem;
  line-height: 1.5;
  margin-bottom: 1rem;
}

.labs-hub-card__status {
  display: inline-block;
  font-size: 0.65rem;
  color: #666;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  padding: 0.2rem 0.6rem;
  background: rgba(255,255,255,0.04);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 100px;
}

.labs-hub-card__action {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  color: #dbb978;
  font-size: 0.85rem;
  font-weight: 600;
  letter-spacing: 0.02em;
}

@media (max-width: 768px) {
  .labs-hub-hero {
    margin-top: 2rem;
  }
  .labs-hub-grid {
    grid-template-columns: 1fr;
  }
}
```

**3. Update `src/App.jsx`:**

Add the LabsHub lazy import near the existing Scratchpad/LabsCanvas imports:

```jsx
const LabsHub = lazyRetry(() => import('./pages/labs/LabsHub'));
```

Add the `/starseed/labs` route. It should appear BEFORE the scratchpad and canvas routes (more specific routes should come after, but React Router v6 handles this automatically -- just place it near the other starseed routes):

```jsx
<Route path="/starseed/labs" element={
  <Suspense fallback={<LazyFallback label="Loading Labs..." />}>
    <LabsHub />
  </Suspense>
} />
```

**4. Update `src/components/TodayRail.jsx`:**

Find the Link on line 133 that currently reads:

```jsx
<Link to="/starseed" className="today-card__cta today-card__cta--starseed">
  Start in Starseed <ArrowRight size={14} />
</Link>
```

Replace it with:

```jsx
<Link
  to={`/starseed/labs/scratchpad?prompt=${encodeURIComponent(todayData.prompt.text)}`}
  className="today-card__cta today-card__cta--starseed"
>
  Start in Starseed <ArrowRight size={14} />
</Link>
```

This passes the daily creative prompt text as a URL-encoded query param. The Scratchpad component (from Plan 02) already handles `?prompt=` with query-param hygiene (respects existing drafts).
  </action>

  <verify>
    <automated>cd C:/dev/jarowe && npm run build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `src/pages/labs/LabsHub.jsx` exists and contains `starseed-shell`, `data-brand="starseed"`, three tool cards (scratchpad, canvas, brainstorm)
    - `src/pages/labs/LabsHub.jsx` renders brainstorm card with `coming-soon` status
    - `src/pages/labs/LabsHub.css` contains `labs-hub-grid`, `labs-hub-card`, `labs-hub-card--disabled`
    - `src/App.jsx` contains `lazyRetry(() => import('./pages/labs/LabsHub'))` and route for `/starseed/labs`
    - `src/components/TodayRail.jsx` contains `starseed/labs/scratchpad?prompt=` and `encodeURIComponent`
    - The old `/starseed` CTA link is replaced (no more `to="/starseed"` on the CTA line)
    - `npm run build` succeeds
  </acceptance_criteria>

  <done>Labs hub page with 3 cards renders at /starseed/labs. Scratchpad and Canvas cards link to their tools, Brainstorm shows "Coming soon". TodayRail CTA links to scratchpad with prompt pre-loaded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Starseed Hub and Labs experience</name>
  <files>none (visual verification only)</files>

  <read_first>
    - No files to read -- this is a human verification checkpoint
  </read_first>

  <action>
Present the verification checklist to the user and wait for approval. This checkpoint verifies the complete Phase 5 experience across all 3 plans.

Verification steps:
1. Run `npm run dev` and visit http://localhost:5173/starseed
2. Verify 4 project cards: BEAMY, AMINA, DECKIT, Starseed Labs -- each with icon, description, tags
3. Click BEAMY card -- should navigate to /projects/beamy
4. Click Starseed Labs card -- should navigate to /starseed/labs
5. Verify AMINA and DECKIT show "Coming soon" and are not clickable
6. Scroll down to contact section -- verify mailto link to jared@starseed.llc
7. On /starseed/labs, verify 3 cards: Scratchpad, Canvas, Brainstorm
8. Verify Brainstorm shows "Coming soon" and is not clickable
9. Click Scratchpad -- navigate to /starseed/labs/scratchpad
10. Type some markdown, wait 2+ seconds, reload page -- content should persist
11. Go back to /starseed/labs, click Canvas -- navigate to /starseed/labs/canvas
12. Draw some shapes, wait 2+ seconds, reload page -- drawing should persist
13. Open DevTools Network tab, navigate to /starseed (not /starseed/labs) -- verify no Milkdown or Excalidraw chunks load
14. Return to homepage, find "Start in Starseed" CTA in the creative prompt card -- click it
15. Verify it navigates to /starseed/labs/scratchpad with the prompt text pre-loaded
16. Check that navbar is hidden on all /starseed/* routes (already handled by isStarseedRoute)

Resume signal: Type "approved" or describe any issues to fix.
  </action>

  <verify>
    <automated>echo "Human verification checkpoint -- no automated test"</automated>
  </verify>

  <acceptance_criteria>
    - User confirms all 16 verification steps pass
    - OR user describes specific issues that need fixing
  </acceptance_criteria>

  <done>User has verified the complete Starseed Hub and Labs experience works end-to-end across all routes, editors, persistence, and navigation.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- All 3 Labs routes render with Starseed nav chrome and dark theme
- Scratchpad auto-saves to localStorage (key: jarowe_labs_scratchpad)
- Canvas auto-saves to localStorage (key: jarowe_labs_canvas)
- Labs hub shows 3 cards with correct active/disabled states
- TodayRail CTA navigates to scratchpad with prompt query param
- Network tab confirms lazy-load isolation (no editor chunks on /starseed)
</verification>

<success_criteria>
- LABS-06: Labs hub page at /starseed/labs with 3 entry point cards (scratchpad, canvas, brainstorm)
- TodayRail CTA connects daily creative prompts to the scratchpad tool
- All pages share Starseed nav chrome and visual identity
- Human verification confirms the full end-to-end experience works
</success_criteria>

<output>
After completion, create `.planning/phases/05-starseed-hub-labs/05-03-SUMMARY.md`
</output>
