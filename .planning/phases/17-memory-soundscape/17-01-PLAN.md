---
phase: 17-memory-soundscape
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/memoryScenes.js
  - src/hooks/useSoundscape.js
  - public/audio/soundscapes/README.md
  - scripts/generate-soundscape-placeholders.mjs
autonomous: true
requirements:
  - PORTAL-03

must_haves:
  truths:
    - "useSoundscape hook loads ambient audio layers via Howler.js (never html5:true, never createMediaElementSource)"
    - "Gain envelope system provides fade-in/fade-out transitions when entering/leaving a memory scene"
    - "memoryScenes.js exports a scene registry with soundscape config for at least syros-cave"
    - "Hook respects global mute state from sounds.js getMuted()"
    - "Audio layers can be individually configured with volume, loop, and fade durations"
    - "Hook ducks site music via AudioContext.duckForNodeAudio when soundscape starts"
    - "Build succeeds with no errors (npx vite build)"
  artifacts:
    - path: "src/hooks/useSoundscape.js"
      provides: "React hook for ambient soundscape playback with gain envelopes"
      exports: ["useSoundscape"]
    - path: "src/data/memoryScenes.js"
      provides: "Memory scene registry with soundscape config"
      exports: ["memoryScenes", "getScene"]
    - path: "public/audio/soundscapes/README.md"
      provides: "Documentation for soundscape audio assets"
    - path: "scripts/generate-soundscape-placeholders.mjs"
      provides: "Node.js script to generate placeholder WAV files via Web Audio offline rendering"

tasks:
  - id: 17-01-01
    title: "Create memoryScenes.js scene registry with soundscape config"
    file: src/data/memoryScenes.js
    description: >
      Create the scene registry data file with soundscape configuration for syros-cave.
      Each scene defines ambient audio layers (drone, texture, detail) with volume,
      loop, fadeIn/fadeOut durations. The syros-cave scene represents the cave grotto
      in Syros, Greece from the family's 3-month worldschooling trip.

  - id: 17-01-02
    title: "Create useSoundscape.js hook with gain envelope system"
    file: src/hooks/useSoundscape.js
    description: >
      React hook that takes a sceneId, looks up its soundscape config from memoryScenes,
      and manages Howler.js instances for each audio layer. Provides gain envelope
      fade-in on mount and fade-out on unmount. Ducks site music via AudioContext.
      Respects mute state. Cleans up all Howl instances on unmount.

  - id: 17-01-03
    title: "Create audio asset directory and placeholder generation"
    files:
      - public/audio/soundscapes/README.md
      - scripts/generate-soundscape-placeholders.mjs
    description: >
      Create the soundscapes directory under public/audio with a README documenting
      what real assets should sound like. Create a Node.js script that generates
      minimal placeholder WAV files using offline AudioContext rendering.
---
